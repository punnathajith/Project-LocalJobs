// import { Router } from "express";
// import { container } from "tsyringe";
// import { CompanyController } from "../controllers/CompanyController";

// const router = Router();
// const companyController = container.resolve(CompanyController);

// router.post("/register", (req, res) => companyController.register(req, res));
// router.post("/login", (req, res) => companyController.login(req, res));
// router.post("/refresh", (req, res) => companyController.refresh(req, res));

// export default router;
